# MCP Agent shared dependencies
# Used by position-monitor, trade-executor, market-scanner

# MCP SDK
mcp>=1.0.0

# Web server for SSE transport
starlette>=0.36.0
uvicorn>=0.27.0
sse-starlette>=2.0.0

# Database
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Configuration
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Data Analysis (needed by scanner + monitor)
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.11.0

# HTTP
aiohttp>=3.9.0
httpx>=0.25.0
requests>=2.31.0

# Claude API (monitor uses Haiku for consultations)
anthropic>=0.39.0

# Moomoo SDK (trade-executor + monitor)
moomoo-api>=1.3.0

# Timezone
python-dateutil>=2.8.2

# Neural inference (cerebellum - ONNX models)
onnxruntime>=1.17.0
