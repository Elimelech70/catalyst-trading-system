# Catalyst Neural — Dependencies
# Install: pip install -r requirements.txt

# ── Data Collection ──
yfinance>=0.2.36          # Yahoo Finance (candles, macro, sectors) — free, no API key
requests>=2.31.0          # HTTP client for news APIs

# ── Configuration ──
python-dotenv>=1.0.0      # load .env file

# ── Storage ──
# sqlite3 is built into Python — no extra dependency

# ── Training (PyTorch + CUDA) ──
# Install PyTorch separately for your CUDA version:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# Check https://pytorch.org/get-started/locally/ for your exact CUDA version
#
# For CUDA 13.0 (RTX 4050):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   (use closest available CUDA version)

# ── Training utilities ──
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.3.0       # train/test split, metrics
matplotlib>=3.8.0         # training visualisation
tensorboard>=2.15.0       # training monitoring (optional)

# ── Model export ──
onnx>=1.15.0              # export for droplet deployment
onnxruntime>=1.16.0       # CPU inference on droplet
